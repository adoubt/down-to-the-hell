[gd_scene format=3 uid="uid://c3roih3aa2an3"]

[ext_resource type="Environment" uid="uid://f5b0km3n8yim" path="res://assets/Materials/night_sky.tres" id="1_cyojo"]

[sub_resource type="GDScript" id="GDScript_kk62l"]
script/source = "extends CharacterBody3D
class_name Player

const SPEED = 6.0
const JUMP_VELOCITY = 12.5
const MOUSE_SENS = 0.002
const PITCH_MIN = deg_to_rad(-80)
const PITCH_MAX = deg_to_rad(80)

var pitch := 0.0

@onready var pivot: Node3D = $CameraPivot

func _ready() -> void:
	Input.set_mouse_mode(Input.MOUSE_MODE_CAPTURED)
	
func _input(event: InputEvent) -> void:
	if event is InputEventMouseMotion:
		# горизонт — вращает всё тело
		rotate_y(-event.relative.x * MOUSE_SENS)
		
		# вертикаль — только камера
		pitch -= event.relative.y * MOUSE_SENS
		pitch = clamp(pitch, PITCH_MIN, PITCH_MAX)
		pivot.rotation.x = pitch

func _physics_process(delta: float) -> void:
	if not is_on_floor():
		velocity += get_gravity() * delta

	if Input.is_action_just_pressed(\"jump\") and is_on_floor():
		velocity.y = JUMP_VELOCITY

	var input_dir := Input.get_vector(\"move_left\", \"move_right\", \"move_forward\", \"move_back\")
	var direction := (transform.basis * Vector3(input_dir.x, 0, input_dir.y)).normalized()

	if direction:
		velocity.x = direction.x * SPEED
		velocity.z = direction.z * SPEED
	else:
		velocity.x = move_toward(velocity.x, 0, SPEED)
		velocity.z = move_toward(velocity.z, 0, SPEED)

	move_and_slide()
"

[sub_resource type="CapsuleShape3D" id="CapsuleShape3D_cyojo"]

[node name="Player" type="CharacterBody3D" unique_id=1885852441]
script = SubResource("GDScript_kk62l")

[node name="CameraPivot" type="Node3D" parent="." unique_id=1595069572]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 1.5650535, -0.1012401)

[node name="Camera3D" type="Camera3D" parent="CameraPivot" unique_id=198260861]
environment = ExtResource("1_cyojo")
current = true

[node name="Model" type="Node3D" parent="." unique_id=677388566]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 1, 0)

[node name="CollisionShape3D" type="CollisionShape3D" parent="." unique_id=1258922805]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 1, 0)
shape = SubResource("CapsuleShape3D_cyojo")
